name: Robustez modelos região e fractional jan-fev

on:
  pull_request:
    paths:
      - '.github/workflows/robustez-modelos-regiao-fractional-jan-fev.yml'
      - 'research/dependencia-fornecedores-compras-publicas/scripts/robustez_modelos_regiao_fractional_jan_fev_2025.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  robustness:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || 'research/robustez-modelos-fracionarios-jan-fev' }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        working-directory: research/dependencia-fornecedores-compras-publicas
        run: pip install -r requirements.txt
      - name: Run robustness
        working-directory: research/dependencia-fornecedores-compras-publicas
        run: python scripts/robustez_modelos_regiao_fractional_jan_fev_2025.py
      - name: Show summary
        working-directory: research/dependencia-fornecedores-compras-publicas
        run: cat results/robustez_modelos_regiao_fractional_jan_fev_2025/resumo_robustez_modelos.json
      - name: Commit outputs
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/dependencia-fornecedores-compras-publicas/results/robustez_modelos_regiao_fractional_jan_fev_2025
          if git diff --cached --quiet; then
            echo 'No robustness changes.'
          else
            git commit -m 'Adiciona robustez região e fractional logit [skip ci]'
            git push origin HEAD:${{ github.head_ref || 'research/robustez-modelos-fracionarios-jan-fev' }}
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: robustez-modelos-regiao-fractional
          path: research/dependencia-fornecedores-compras-publicas/results/robustez_modelos_regiao_fractional_jan_fev_2025
